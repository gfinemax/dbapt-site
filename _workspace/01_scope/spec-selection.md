# Spec Selection

- Selected approved spec path: Existing public library index scope and repository UI rules in `AGENTS.md`.
- Implementation boundary: Adjust the logged-in side CTA presentation and matching drawer title text without changing access rules. On `/library`, logged-in users may see approved uploaded document entries and open them through the existing authenticated PDF viewer route.
- Request/spec conflicts: none. The requested badge placement/orientation change is visual only, and uploaded document opening remains inside the existing authenticated document access boundary.
- Planning may continue: yes
