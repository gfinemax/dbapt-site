# Spec Selection

## Selected Approved Spec

`docs/superpowers/specs/2026-05-25-daebang-housing-cooperative-portal-design.md`

## Implementation Boundary

This pass changes only the public `조합소개` page by replacing the redirect-prone Naver Maps search iframe with a Naver Maps JavaScript SDK surface, pinning the marker to the verified office coordinate without client-side geocoding, adding a selectable detailed route guide with a blinking `+ 자세히보기` affordance, removing the consultation-hours and parking rows, removing the specific trust-company name from the partner card, revising the history-section headline/support copy toward renewed transparent cooperative-led execution, adding the `2026.04.18` regular general meeting milestone, removing the `2013.05.01` leader-change milestone, and preserving the external Naver Maps link. Navigation, authentication boundaries, greeting content, and private-service scope remain unchanged.

## Conflicts

none

## May Planning Continue

yes
